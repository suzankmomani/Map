<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RatingBarStyle" parent="Theme.AppCompat">
        <item name="colorControlActivated">@color/active_rating</item>
        <item name="colorControlNormal">@color/active_rating</item>
        <item name="android:colorControlNormal">@color/non_active_rating</item>
        <item name="android:colorControlActivated">@color/active_rating</item>
    </style>
</resources>